apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: camunda-qa
resources:          # replaces bases
  - ../base/repo-a
  - ../base/repo-b
patches:
  - path: configmap-patch-repo-a.yaml
  - path: configmap-patch-repo-b.yaml
